// @ts-check

/** @type {HTMLElement | null} */
const app = document.querySelector("#app");

if (app instanceof HTMLElement) {
  app.textContent = "AeroBeat gameplay package foundation only; runtime scenes arrive in Task 6.";
}
