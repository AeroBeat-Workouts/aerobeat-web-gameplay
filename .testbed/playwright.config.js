// @ts-check

/**
 * Playwright config placeholder.
 *
 * Browser-visible gameplay tests must fail on unexpected console warning/error
 * output. Add the console listener in test setup before enabling real tests.
 *
 * @type {{ testDir: string }}
 */
export default {
  testDir: "./test"
};
